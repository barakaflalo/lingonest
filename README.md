# LingoNest — מבנה האפליקציה ומדריך הרחבה

**LingoNest** · ללמוד לדבר, בכל מקום · AppNest · Barak Aflalo
גרסה נוכחית: **1.18.0**

---

## מפת הקבצים

כל הקבצים שטוחים בשורש הריפו ובאותיות קטנות. כל הקבצים שייכים **לאותה גרסה**, ולכן תמיד מעלים את כולם יחד.

| קובץ | תפקיד | מתי נטען |
|---|---|---|
| `index.html` | מעטפת: CSP, גופנים, סדר טעינה, רשת ביטחון לרענון | תמיד |
| `style.css` | עיצוב, 5 ערכות צבע, יום/לילה, הדפסה | תמיד |
| `content.js` | רשימת השפות, **רשימת המושגים** (מזהה, נושא, רמה, פירוש בעברית ובאנגלית), נושאים, דיאלוגים, מנגנון הרישום | תמיד, ראשון |
| `numbers.js` | מנוע "מספר ← מילים" (הכללי) | תמיד |
| `ui-en.js` | טקסטי ממשק באנגלית (גיבוי לכל מפתח חסר) | תמיד |
| `assistant-map.js` | "המפה" של עוזר AppNest עבור LingoNest: מסכים, שדות, פעולות בטוחות, מצב חי, וגישה למפתח המוצפן | תמיד, לפני `app.js` |
| `app.js` | המנוע: מסכים, שיעורים, חזרות מרווחות, הקראה, זיהוי דיבור, בינה, בדיקת איכות, גיבוי, טוען החבילות | תמיד |
| `features.js` | שיחה עם בינה, ערכת טיול, טיפים ודקדוק, יעד שבועי ותגים. **קורא ל-`boot()` בסוף** — המודול האחרון של האפליקציה | תמיד, אחרי `app.js` |
| `help.js` | תוכן מדריך האפליקציה (עברית + אנגלית) — כל פיצ׳ר חדש מקבל כאן סעיף | כשפותחים את המדריך |
| `appnest-assistant.js` | **מנוע עוזר AppNest המשותף — זהה בייט-בבייט בכל האפליקציות. לא עורכים אותו כאן.** משנים רק את `assistant-map.js` | תמיד, אחרון |
| `ui-he.js` `ui-ru.js` `ui-es.js` `ui-ar.js` | טקסטי ממשק לכל שפת ממשק | רק שפת הממשק שנבחרה |
| `lang-th.js` `lang-ar.js` `lang-ru.js` `lang-es.js` `lang-en.js` `lang-fr.js` `lang-ary.js` `lang-it.js` `lang-pt.js` `lang-ro.js` `lang-yi.js` `lang-de.js` `lang-el.js` `lang-ja.js` `lang-zh.js` `lang-hi.js` `lang-tr.js` `lang-ko.js` `lang-sw.js` `lang-am.js` `lang-ha.js` `lang-yo.js` `lang-ig.js` `lang-tw.js` `lang-lg.js` `lang-ka.js` `lang-vi.js` `lang-id.js` | **חבילת שפה**: אלפבית, תנועות, טונים, כל התרגומים, מספרים ומטבע, צורות זכר/נקבה, טיפי דקדוק, מספרי חירום | שפת הלימוד מיד, השאר ברקע |
| `sw.js` | עבודה אופליין. שומר את כל הקבצים במכשיר | — |
| `manifest.json`, `icon-*.png`, `privacy_policy.html`, `.nojekyll` | PWA ו-GitHub Pages | — |

## איך זה עובד

- **טעינה לפי צורך.** בפתיחה נטענים רק הליבה, שפת הממשק ושפת הלימוד. שאר השפות נטענות ברקע אחרי שנייה, כדי שאחוזי ההתקדמות יוצגו ושהכול יעבוד גם אופליין.
- **גרסה על כל קובץ.** כל קובץ נטען עם `?v=גרסה`, ובתוכו מוטבע אותו מספר גרסה. אם הטלפון מחזיק קובץ ישן מהמטמון, מופיע פס **"העדכון לא נטען במלואו — לחץ לרענון"**, במקום כפתורים שלא עובדים. אם `app.js` בכלל לא נטען, רשת הביטחון ב-`index.html` מציגה את אותו פס אחרי 6 שניות.
- **ההתקדמות נשמרת לפי מזהה קבוע.** מילים נשמרות כ-`W:<id>`, אותיות כ-`L:<מספר>`, תנועות כ-`V:<מספר>` ומילים שלי כ-`C:<id>`. **אסור לשנות מזהה קיים** — זה מאבד את ההתקדמות שלו. מותר להוסיף, לתקן תרגום או לשנות רמה ונושא.
- **האבחון בהגדרות** מציג את כל המודולים שנטענו עם הגרסאות שלהם, ואת 10 השגיאות האחרונות.

## להוסיף מילים או משפטים

1. ב-`content.js`, במערך `CONCEPTS`, מוסיפים שורה:
   `["market2","shop",7,"דוכן","Stall"]` — מזהה ייחודי, נושא, רמה (1–8), פירוש בעברית, פירוש באנגלית.
2. בכל אחד מקבצי `lang-xx.js`, בתוך `words`, מוסיפים שורה אחת:
   `market2|แผงลอย|phaeng loi|פאנג לוי` — מזהה | טקסט בשפה | תעתיק לטיני | הגייה באותיות עבריות.
3. נושא חדש: מוסיפים אותו ל-`WORD_CATS` (או `PHRASE_CATS` למשפטים), אייקון ב-`CAT_ICON`, ומפתח `cat_<נושא>` בכל חמשת קבצי `ui-xx.js`.

## שפה בלי קול / קטגוריה ייחודית

- חבילה עם `voiceFallback: 'heb'` תשתמש בקול של השפה אם יש כזה במכשיר, ואם אין — תקריא את עמודת ההגייה בקול העברי.
- חבילה עם `speakHeb: true` תוקרא מעמודת ההגייה בעברית, בקול העברי (כמו ביידיש). כדי שזה יעבוד, ב-`LANGS` מגדירים `tts: "he-IL"`.
- קטגוריה שקיימת רק בחלק מהשפות: מוסיפים אותה ל-`ONLY_LANG` ב-`content.js`. שפות אחרות פשוט לא יציגו אותה.

## להוסיף שפת לימוד חדשה (למשל צרפתית)

1. ב-`content.js`, באובייקט `LANGS`, מוסיפים שורה (ובנוסף תג ב-`LANG_BADGE` ואזור ב-`LANG_GROUPS`):
   `fr: { flag: "🇫🇷", tts: "fr-FR", dir: "ltr", native: "Français", name: { he: "צרפתית", en: "French", ru: "Французский", es: "Francés", ar: "الفرنسية" } }`
2. מעתיקים את `lang-es.js` ל-`lang-fr.js`, משנים את הקוד ל-`'fr'`, ומחליפים אלפבית, תרגומים, צורות נקבה ומטבע.
   את ההערה למסך האותיות אפשר לתת בתוך החבילה: `note: { he: "...", en: "..." }`.
3. מספרים: בסוף הקובץ מגדירים `NUM.fr = { tokens(n, withCurrency) {...} }`, לפי הדוגמה של ספרדית.
4. זהו. `sw.js` מקבל את רשימת הקבצים אוטומטית בבנייה, והמסכים מציגים את השפה החדשה לבד.

## הוצאת גרסה

1. מעלים את מספר הגרסה (`VER` בסקריפט הבנייה). הוא מוטבע אוטומטית ב-`?v=`, בכל הקבצים וב-`sw.js`.
2. בדיקות לפני מסירה: תחביר בכל קובץ; כל מפתח קיים ב-5 שפות הממשק; כל מושג מתורגם בכל חבילה; מנוע המספרים; סיור אוטומטי בכל המסכים ב-5 שפות. בנוסף בודקים עדכון מגרסה קודמת, חבילה חסרה, גרסה לא תואמת, ו-`app.js` חסר.
3. מעלים **את כל הקבצים יחד** לריפו.
4. בטלפון: אם משהו נראה ישן, סוגרים את האפליקציה לגמרי ופותחים מחדש, או לוחצים על פס הרענון.

## עוזר AppNest

המנוע (`appnest-assistant.js`) מועתק כמו שהוא מהגרסה הרשמית. כל ההתאמה ל-LingoNest נמצאת ב-`assistant-map.js`.
המפתח ב-LingoNest מוצפן, ולכן `readAiConfig` מחזיר את המפתח שפוענח בזיכרון בזמן העלייה. אם אין מפתח, העוזר מבקש לחבר אחד.
כשמשפרים את המנוע, מעתיקים את הקובץ החדש כמו שהוא, בלי לגעת במפה.

## תיקוני תוכן מבדיקת האיכות

תיקון שהוחל נשמר במכשיר (`st.fix`) וגובר על קובץ השפה. את הדוח מעתיקים ושולחים, ואז מכניסים את התיקונים לקבצי `lang-xx.js` בגרסה הבאה.

## היסטוריה
- **1.18.0** — מדריך אפליקציה מלא (`help.js`, נטען לפי צורך; מסך `help` עם חיפוש וכפתורי מעבר), מדיניות פרטיות מעודכנת לכל פיצ׳רי הבינה.
- **1.17.1** — מסך בחירת השפה לא גולש הצידה בטלפון; שמות השפות בכתב המקורי מבודדים (bdi); ברכה עם מייל מציגה רק את השם.
- **1.17.0** — בחירת שפה חדשה: כרטיס שפה בדף הבית + שפות אחרונות, ומסך `langs` עם חיפוש, "השפות שלי" ואזורים (`LANG_GROUPS`), ותג בכתב של השפה (`LANG_BADGE`) במקום דגלים שלא מוצגים ב-Windows.
- **1.16.0** — גאורגית (lang-ka.js), וייטנאמית (lang-vi.js), אינדונזית (lang-id.js). 28 שפות.
- **1.15.0** — טווי (lang-tw.js, גאנה) ולוגנדה (lang-lg.js, אוגנדה). 25 שפות.
- **1.14.0** — ניגריה: האוסה (lang-ha.js), יורובה (lang-yo.js), איגבו (lang-ig.js). ביורובה מחירים באנגלית (כמו בשוק), מספרים ביורובה ברשימת המילים. 23 שפות.
- **1.13.0** — סווהילית (lang-sw.js) ואמהרית (lang-am.js). מנגנון `voiceFallback: 'heb'`: אם אין במכשיר קול לשפה, ההקראה עוברת להגייה בעברית בקול העברי (עם הודעה). 20 שפות.
- **1.12.0** — טורקית (lang-tr.js) וקוריאנית (lang-ko.js). 18 שפות.
- **1.11.0** — יפנית (lang-ja.js), סינית (lang-zh.js), הינדי (lang-hi.js). חבילה יכולה לתת כותרת משלה למקטע התנועות (`vowelsTitle` — קטקאנה / סיומות), הסבר טונים (`toneNote`, `twisterNote`), ושפות בלי רווחים (`NOSPACE`) מחולקות למילים ב-Intl.Segmenter. 16 שפות.
- **1.10.0** — גרמנית (lang-de.js) ויוונית (lang-el.js). 13 שפות.
- **1.9.0** — יידיש (lang-yi.js): 407 פריטים + 22 ביטויים ייחודיים (קטגוריה `yid` שקיימת רק ביידיש — `ONLY_LANG`). אין קול יידיש, ולכן `speakHeb` מקריא את עמודת ההגייה בקול העברי (`LANGS.yi.tts = he-IL`).
- **1.8.0** — איטלקית (lang-it.js), פורטוגזית ברזילאית (lang-pt.js), רומנית (lang-ro.js). 10 שפות.
- **1.7.0** — שתי שפות חדשות: צרפתית (lang-fr.js) ומרוקאית/דארִיג׳ה (lang-ary.js), כל אחת עם 407 פריטים, אלפבית (+צירופי צלילים בצרפתית, ڭ/ڤ במרוקאית), מספרים ומחירים (אירו / דירהם), טיפים ומספרי חירום.
- **1.6.0** — שיחה עם בינה בשפת היעד (7 מצבים, 2 רמות, תיקונים, הצעות תשובה, משוב והוספת משפטים), ערכת טיול (מספרי חירום לפי מדינה, כרטיסים אישיים עם תרגום, משפטי חירום ומועדפים — אופליין), 6 טיפי דקדוק ותרבות לכל שפה (בחבילת השפה), יעד שבועי ו-16 תגים.
- **1.5.2** — תיקוני תאית מדוח בדיקת האיכות הראשון (בבקשה, מה שלומך, 1/100/1000, כחול, יום חמישי, 4 כיווני אוויר, תנועות แ/เ-ิ בהגייה העברית), "סגנון הבית" של ההגייה נשלח לבודק כדי שלא יסמן בחירות מכוונות, והתקדמות בדיקה ישנה מתאפסת בגרסה חדשה.
- **1.5.1** — בדיקת האיכות עמידה לעומס בשרתי הבינה: מעבר אוטומטי למודל אחר, המתנה סבלנית עם ספירה לאחור, ושמירת התקדמות עם כפתור "המשך".
- **1.5.0** — עוזר AppNest (המנוע המשותף + מפה ל-LingoNest), בדיקת איכות תוכן עם בינה (תיקונים מקומיים + דוח), בניית משפטים (גם בתוך השיעורים; בתאית עם חלוקה חכמה למילים), הכתבה, כרטיסיות בהחלקה.

- **1.4.0** — בסיס מודולרי: חבילת שפה לכל שפה שנטענת לפי צורך, גרסה על כל קובץ, הגנה מעדכון חלקי, אבחון מודולים, מנוע מספרים אחיד.
- **1.3.0** — דיאלוגים (5 מצבים, "התפקיד שלי"), מחירים ומספרים עם הקראה, "נחש את המחיר", נושאים: מזג אוויר, מקצועות, רגשות, כיוונים. 407 פריטים לשפה.
- **1.2.0** — פי 2 תוכן: חיות, פירות, ירקות, מאכלים, מקומות, חפצים, פעלים ותארים, 40 משפטים שלמים לפי מצבים. 9 רמות.
- **1.1.0** — תנועות וטונים בתאית, סימני תנועה בערבית, חיפוש "איך אומרים", תרגול דיבור, צורות זכר/נקבה.
- **1.0.0** — גרסה ראשונה: 5 שפות, אותיות, מילות הישרדות, משפטים, "דבר בשבילי", חזרות מרווחות, חיבור בינה (BYOK).
